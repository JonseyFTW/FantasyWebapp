providers = ["node"]

[build]
cmds = [
  "cd ../shared && npm ci && npm run build",
  "npm ci",
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"